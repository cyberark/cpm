---

- hosts: windows
  connection: local
  
  vars_prompt:
    - name: vault_password
      prompt: "Please enter the Vault password"
      private: yes
      encrypt: "sha512_crypt"
  
  
  tasks:
    
    - include_role: 
        name: ../cpm
